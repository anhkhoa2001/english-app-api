#config kafka
server:
  port: 9001
  servlet:
    context-path: /api
spring:
  datasource:
    password: 1
    url: jdbc:postgresql://localhost:5432/user_service?currentSchema=user_schema
    username: khoadt
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        implicit-strategy: org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
  redis:
    host: localhost
    port: 6379

kong:
  api-key: JPrxbq2bbX6nJTJbu7INIameRUH6dRZn
